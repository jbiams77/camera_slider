{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "scp",
            "type": "shell",
            "command": "scp ./*.py pi@192.168.1.117:/home/pi/camera_slider",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}